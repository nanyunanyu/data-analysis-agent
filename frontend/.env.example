# Backend service used by the Vite dev proxy.
# Keep this aligned with the uvicorn --port value used by the backend.
VITE_BACKEND_PORT=8003

# Use this instead of VITE_BACKEND_PORT when the backend is not on localhost.
# VITE_BACKEND_URL=http://localhost:8003
